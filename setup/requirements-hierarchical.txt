# Optional dependencies for hierarchical RL with LLMs and Minecraft
# These are large packages and are not included in the base Docker image by default.
# Install with: pip install -r setup/requirements-hierarchical.txt

# L2P dependencies (vendor/l2p) - LLM-to-PDDL planning framework
# These are required by the vendored l2p library for PDDL parsing and formatting.
retry
pddl
typing_extensions
pyyaml

# Ollama - Python client for local LLM inference via Ollama server
# Note: This is just the client library. The Ollama server runs separately,
# either as a system service or in its own Docker container.
# See docker-compose.yml for optional Ollama service configuration.
# See: https://github.com/ollama/ollama-python
ollama>=0.1.0

# Anthropic - Python client for Claude API
# See: https://github.com/anthropics/anthropic-sdk-python
anthropic>=0.39.0

# OpenAI - Python client for OpenAI API (used by vendored l2p library)
openai>=1.0.0

# tiktoken - Token counter for OpenAI models (required by l2p OPENAI connector)
tiktoken>=0.5.0

# Pillow - Image processing library (needed for vision LLM integration)
Pillow>=10.0.0

# MineLand - Multi-agent Reinforcement Learning in Minecraft
# MineLand is automatically installed from GitHub when HIERARCHICAL_MODE=true
# System requirements (installed by Dockerfile):
#   - Python 3.11+ (required by MineLand)
#   - Node.js 18.18.x  
#   - Java JDK 17
# See: https://github.com/cocacola-lab/MineLand
# MineLand Python dependencies (pre-installed):
gymnasium>=0.28.0
numpy>=1.24.0
psutil>=5.9.0
tqdm>=4.65.0
